# หลักฐาน Checkpoint สัปดาห์ที่ 7 — ล็อกอิน + ACL + กุญแจคลังข้อมูล + ขึ้นออนไลน์

โฟลเดอร์นี้เก็บ screenshot หลักฐานของสัปดาห์ที่ 7 แยกจาก `docs/Checkpoint 1-4.*`
ของสัปดาห์ที่ 6 (เลขหัวข้อซ้ำกัน แต่คนละสัปดาห์ จึงแยกโฟลเดอร์กันไว้)

**ชื่อไฟล์ที่ใช้ในโฟลเดอร์นี้:**

- `Checkpoint 3.png` — มีไฟล์ ACL.md + ปุ่มต่างกันตามบทบาท + ไม่ล็อกอินอ่านไม่ได้
  (permission-denied ตอนไม่ล็อกอิน)
- `Checkpoint 4-1.png`, `Checkpoint 4-2.png`, ... — เพื่อนเปิด URL ของคุณได้จากเครื่องเขา
  และต้องล็อกอินก่อนจึงเห็นข้อมูล (แนบได้หลายภาพ ตั้งชื่อไล่เลข -1, -2, -3 ต่อไปได้)
