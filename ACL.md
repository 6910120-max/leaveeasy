# ACL.md — ตารางสิทธิ์ (Access Control List) ของ LeaveEasy

> อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 (ผู้ใช้ 3 บทบาท) และหัวข้อ 6 (กฎการเปลี่ยนสถานะใบลา)
> ค่าที่เก็บในช่อง `role` ของ Firestore (`users/{uid}.role`) ต้องเป็นรหัสอังกฤษเสมอ: `employee` / `manager` / `hr`

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **ผู้ขอลา** (`employee`) | • ยื่นใบขอลาใหม่ (บันทึก `requesterId` เป็น uid ของตัวเอง)<br>• ดูใบลาของตัวเองเท่านั้น<br>• เขียนความเห็นเพิ่มในใบของตัวเอง<br>• ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | • **เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) ไม่ว่าใบของใคร — รวมถึงใบของตัวเองด้วย**<br>• **เปิดดูใบลาของผู้ขอลาคนอื่น**<br>• แก้ไขประเภทการลา<br>• ลบใบลาที่สถานะไม่ใช่ `รอพิจารณา` แล้ว หรือใบของคนอื่น<br>• กำหนดผู้อนุมัติให้ใบลา |
| ✅ **ผู้อนุมัติ** (`manager`) | • ดูใบลาทุกใบของทุกคน<br>• เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) ของใบที่ยังรอพิจารณา — **แก้เฉพาะช่อง `status` เท่านั้น**<br>• เขียนความเห็นในทุกใบ<br>• ยื่นใบลาของตัวเองได้เหมือนพนักงานทั่วไป (ตอนนั้นสวมบทบาทเป็นผู้ขอลา) | • **เปลี่ยนสถานะใบลาที่ตัวเองเป็นผู้ยื่น** (เช็กจาก `requesterId` ต้องไม่ตรงกับ uid ของตัวเอง) — กันไม่ให้อนุมัติใบของตัวเอง<br>• ลบใบลาของคนอื่น<br>• แก้ไขประเภทการลา<br>• กำหนดผู้อนุมัติให้ใบลา (เป็นสิทธิ์ของ hr เท่านั้น) |
| 🧾 **ฝ่ายบุคคล** (`hr`) | • ทำได้ทุกอย่างที่ผู้อนุมัติ (manager) ทำได้<br>• กำหนดผู้อนุมัติให้แต่ละใบ<br>• เพิ่ม แก้ ลบ ประเภทการลา<br>• เห็นแดชบอร์ดสรุปทั้งระบบ *(หน้าโครงจาก prototype)* | • **เปลี่ยนสถานะใบลาที่ตัวเองเป็นผู้ยื่น** (กฎเดียวกับ manager — ห้ามอนุมัติใบตัวเอง)<br>• ลบใบลาของพนักงานคนอื่น (ลบได้เฉพาะใบของตัวเองตอนที่ยังรอพิจารณา เหมือนบทบาทอื่น)<br>• แก้ไขช่องอื่นของใบลานอกเหนือจาก `status` ตอนกดอนุมัติ/ไม่อนุมัติ |

## กฎเสริมที่ใช้ทุกบทบาท (ไม่ว่า role ไหน)

- เปลี่ยนสถานะได้ครั้งเดียว: จาก `รอพิจารณา` → `อนุมัติ` หรือ `ไม่อนุมัติ` เท่านั้น เปลี่ยนกลับหรือเปลี่ยนข้ามสถานะไม่ได้
- ไม่อนุมัติได้ ต้องมีความเห็นในใบนั้นอย่างน้อย 1 รายการก่อน
- ต้องล็อกอินก่อนเสมอ ไม่ว่าบทบาทใด (ไม่ล็อกอิน = อ่าน/เขียนอะไรไม่ได้เลย)

## หมายเหตุเรื่องช่วงเวลาบังคับใช้จริง

- **สัปดาห์ที่ 7 (ตอนนี้):** ตารางนี้ใช้จำกัด **ปุ่ม/เมนูบนหน้าจอ** เท่านั้น (ซ่อน/ปิดปุ่มตาม `role` ของคนที่ล็อกอินอยู่) — ยังไม่ใช่การป้องกันจริง เพราะคนที่ไม่ผ่านหน้าเว็บ (เช่นเรียก Firestore ตรง ๆ) ยังทำตามช่อง "ทำไม่ได้" ได้อยู่
- **สัปดาห์ที่ 8:** ตารางเดียวกันนี้จะถูกแปลงเป็น **Firestore Security Rules รายห้อง** ซึ่งเป็นประตูที่บังคับใช้ได้จริงในระดับฐานข้อมูล
