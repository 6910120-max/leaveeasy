// ─────────────────────────────────────────────────────────────
// js/ai-config.example.js — แม่แบบไฟล์ตั้งค่าคีย์ผู้ช่วย AI (OpenRouter)
//
// ไฟล์นี้เป็นไฟล์ต้นแบบที่ push ขึ้น GitHub ได้ (ไม่มีคีย์จริง)
// วิธีใช้: คัดลอกไฟล์นี้เป็นชื่อ js/ai-config.local.js แล้วใส่คีย์จริงของคุณแทน
// ไฟล์ที่ลงท้าย .local.js ถูกตั้งไว้ใน .gitignore แล้ว จะไม่ถูก push ขึ้น GitHub
// ─────────────────────────────────────────────────────────────

window.OPENROUTER_API_KEY = "ใส่คีย์ OpenRouter ของคุณตรงนี้ เช่น sk-or-v1-xxxxxxxx";
window.OPENROUTER_MODEL = "google/gemini-2.5-flash-lite";
